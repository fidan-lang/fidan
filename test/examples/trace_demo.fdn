action inner with (certain msg -> string) {
    panic("something went wrong: {msg}")
}

action middle with (certain count -> integer) {
    inner("iteration {count}")
}

action outer {
    middle(42)
}

outer()
